# RAG Chatbot Development - AI Log

This document serves as a comprehensive log of the development phases and tasks completed during this chat session to build a Retrieval-Augmented Generation (RAG) chatbot from scratch.

## Project Philosophy
- **Goal:** Build an educational RAG pipeline step-by-step.
- **Rules:** Keep it simple, readable, modular. Prefer functions over classes. Avoid over-engineering, design patterns, and generic abstractions. Focus on one phase at a time.

---

## Phase 1: Project Setup
- **Objective:** Establish the foundational project structure.
- **Tasks Completed:**
  - Created `config.py` to store configuration constants (API keys, directories, model names).
  - Setup `.env` for secure environment variable loading.
  - Implemented `app.py` with basic Streamlit UI (Page config, Phase indicator, Model Selector, API Key status).
  - Created automatic directory initialization for `uploads`, `logs`, `docs`, and `tests`.

## Phase 2: PDF Upload & Text Extraction
- **Objective:** Build a reliable PDF ingestion module.
- **Tasks Completed:**
  - Created `services/pdf_loader.py` using `pypdf`.
  - Implemented page-by-page text extraction to preserve metadata (filename, page number).
  - Updated `app.py` to allow multiple PDF uploads, save them temporarily, and display extraction progress and summaries without relying on `st.session_state`.

## Phase 3: Custom Chunking
- **Objective:** Break extracted text into manageable, word-aware chunks.
- **Tasks Completed:**
  - Created `services/chunker.py`.
  - Implemented logic to chunk text while respecting word boundaries and skipping empty pages.
  - Generated deterministic chunk IDs (e.g., using `uuid5`).
  - Added chunking constants (`DEFAULT_CHUNK_SIZE`, `DEFAULT_CHUNK_OVERLAP`) to `config.py`.

## Phase 4: Embedding Generation
- **Objective:** Convert text chunks into vector embeddings.
- **Tasks Completed:**
  - Created `services/embedding_service.py`.
  - Integrated Google's GenAI SDK (`text-embedding-004`).
  - Added dynamic embedding dimension detection.
  - Handled rate limiting and API failures gracefully.

## Phase 5: Vector Database (Qdrant)
- **Objective:** Store embeddings and metadata for similarity search.
- **Tasks Completed:**
  - Created `services/vector_db.py`.
  - Implemented logic to connect to Qdrant, create collections (Cosine distance), and upsert vectors.
  - Used deterministic UUIDs to avoid duplicate vectors.
  - Updated Streamlit UI to show Qdrant connection status and indexing metrics.

## Phase 6: Semantic Retrieval
- **Objective:** Search Qdrant for relevant chunks based on user queries.
- **Tasks Completed:**
  - Created `services/retrieval.py`.
  - Implemented question embedding generation and vector similarity search.
  - Added retrieval verification UI in Streamlit to display top chunks and their similarity scores.
  - Handled offline database gracefully.

## Phase 7: Prompt Construction
- **Objective:** Build a structured prompt combining context, system instructions, and user queries.
- **Tasks Completed:**
  - Created `services/prompt_builder.py`.
  - Defined a strict, hallucination-resistant `RAG_PROMPT_TEMPLATE`.
  - Implemented `build_prompt` to format retrieved chunks (including filename, page, text, score).
  - Added a collapsible expander in the UI to inspect the constructed prompt.

## Phase 8: LLM Integration & Response Generation
- **Objective:** Send the prompt to language models (Gemini / Mistral / Qwen) and display the answer.
- **Tasks Completed:**
  - Created `services/llm_service.py` to act as a unified router.
  - Implemented provider-specific private helpers (e.g., `_generate_gemini_response`, `_generate_qwen_response`).
  - Updated the Streamlit UI to measure execution time, present a clean dashboard, and output the final AI answer.

## Bug Fixes & Refinements
- **Document Scope Selector Bug:** Fixed a `TypeError` where `retrieve_relevant_chunks` received an unexpected `filename_filter` argument.
  - *Fix:* Updated the function signature in `services/retrieval.py` and implemented Qdrant filtering (`Filter`, `FieldCondition`, `MatchValue`) to support querying within specific documents.

---

*Log generated by Antigravity AI.*
